language: cpp

# Use containers
sudo: false

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - llvm-toolchain-precise-3.6
    packages:
      - libstdc++6-4.9
      - libllvm3.6
      - llvm-3.6
      - llvm-3.6-dev

compiler:
  - clang

script:
  - make test

notifications:
  email: false
